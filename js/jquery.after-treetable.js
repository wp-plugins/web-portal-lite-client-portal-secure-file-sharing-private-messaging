jQuery( document ).ready( function() {
    var width_table = jQuery( '.wpc_files_tree_content' ).css('width');
    //alert(width_first_column)
    jQuery( '.wpc_files_tree_header' ).css( 'width', width_table );

});
